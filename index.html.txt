<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>RIFA LOS COMPÁS</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #0b3d0b;
      color: white;
      text-align: center;
    }
    img {
      max-width: 250px;
      margin: 20px;
    }
    .info {
      margin: 20px;
      font-size: 18px;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(10, 1fr);
      gap: 8px;
      max-width: 600px;
      margin: auto;
    }
    .ticket {
      padding: 10px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
    }
    .available {
      background: #1abc9c;
    }
    .sold {
      background: #e74c3c;
      cursor: not-allowed;
    }
    .payments {
      margin: 30px;
    }
    button {
      padding: 10px 20px;
      margin: 10px;
      font-size: 16px;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <img src="logo.png" alt="RIFA LOS COMPÁS">

  <div class="info">
    🎟 Boleto: <b>$50</b><br>
    🏆 Premios:<br>
    1er lugar: $5,000<br>
    2do lugar: $1,000<br>
    3er lugar: $500
  </div>

  <h2>Selecciona tu boleto</h2>

  <div class="grid" id="tickets"></div>

  <div class="payments">
    <h3>Formas de pago</h3>
    <button onclick="alert('Pago en efectivo directamente con el organizador')">
      💵 Efectivo
    </button>
    <button onclick="mostrarClabes()">
      💳 Transferencia
    </button>
  </div>

  <script>
    const FORM_URL = "https://forms.gle/ArsXFjn1FGFakDav7";

    const vendidos = [
      // aquí luego vendrán desde Sheets
    ];

    const contenedor = document.getElementById("tickets");

    for (let i = 0; i < 200; i++) {
      const num = i.toString().padStart(3, "0");
      const div = document.createElement("div");

      if (vendidos.includes(i)) {
        div.className = "ticket sold";
        div.innerText = num;
      } else {
        div.className = "ticket available";
        div.innerText = num;
        div.onclick = () => {
          window.location.href = FORM_URL;
        };
      }

      contenedor.appendChild(div);
    }

    function mostrarClabes() {
      alert(
        "TRANSFERENCIA\n\n" +
        "Banco: TU BANCO\n" +
        "CLABE: TU CLABE\n" +
        "Nombre: TU NOMBRE"
      );
    }
  </script>

</body>
</html>
